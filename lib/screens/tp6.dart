import 'package:flutter/material.dart';

class TP6 extends StatelessWidget {
  const TP6({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(centerTitle: true, title: const Text('TP6')),
    );
  }
}
