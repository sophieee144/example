import 'package:flutter/material.dart';

class TP4 extends StatelessWidget {
  const TP4({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(centerTitle: true, title: const Text('TP4')),
    );
  }
}
