import 'package:flutter/material.dart';

class TP5 extends StatelessWidget {
  const TP5({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(centerTitle: true, title: const Text('TP5')),
    );
  }
}
